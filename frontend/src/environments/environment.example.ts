// Copy this file to environment.ts and fill in your own values.
// environment.ts is gitignored — it holds your production Google OAuth client ID.
export const environment = {
  production: true,
  apiUrl: '/api',
  socketUrl: '/',
  googleClientId: 'your-google-oauth-client-id.apps.googleusercontent.com',
};
